import type { Config } from 'tailwindcss';

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        winky: ['Winky Sans', 'sans-serif'],
      },
    },
  },
  plugins: [],
};

export default config;
